---
id: DEMO-0007
title: Add structured logs and an operator-visible runtime status surface
state: Done
priority: 3
depends_on:
  - DEMO-0005
fpf.lifecycle_state: Shaping
fpf.semantic_risk: high
fpf.primary_patterns:
  - A.15.2
  - A.6.C
  - B.5.1
  - F.11
fpf.vocabulary_watch:
  - log
  - run record
  - status snapshot
  - status surface
acceptance:
  - Structured log entries exist for startup, validation, dispatch, hook failure/timeout, worker failure/timeout, and retry events.
  - Issue-related log entries include stable context fields such as `issue_id` or `issue_identifier` when applicable.
  - A status command or machine-readable snapshot reports at least the current running issues and retrying issues from orchestrator state.
validation:
  - dotnet build src/DeliveryHarness.Cli/DeliveryHarness.Cli.fsproj
  - dotnet run --project tests/DeliveryHarness.Tests/DeliveryHarness.Tests.fsproj
constraints:
  - Keep observability optional for correctness; the orchestrator must not depend on a UI.
  - Avoid logging large raw payloads or secrets.
---
# Problem

The observability surface described here has already landed. Host mode now writes structured runtime events and a machine-readable status snapshot so operators can inspect running and retrying work without attaching a debugger.

# Scope

Keep this issue as the historical reference for the current observability contract so later work extends the existing host log/snapshot surfaces instead of re-describing them as missing.

# Notes

Execution plan:
- `docs/exec-plans/active/DEMO-0007.md`

FPF validation:
- lifecycle state: `Shaping`
- admissibility gate: requires `DEMO-0005`
- contract bundle routing: acceptance = promise content, validation = evidence, constraints = admissibility/deontic limits
- vocabulary watch: `log`, `run record`, `status snapshot`, `status surface`

Current baseline:
- `serve` appends structured events to `.harness/runtime/host-events.jsonl`
- `status` reads `.harness/runtime/status.json`
- issue-related log entries carry issue id, attempt number, turn number, and optional run/workspace paths when applicable
- host logs and status snapshots preserve the same secret-redaction posture as other operator-visible output

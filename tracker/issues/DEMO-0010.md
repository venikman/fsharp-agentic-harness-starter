---
id: DEMO-0010
title: Strengthen workspace, process, and secret-handling safety guards
state: Done
priority: 2
depends_on:
  - DEMO-0002
fpf.lifecycle_state: Shaping
fpf.semantic_risk: high
fpf.primary_patterns:
  - A.15.2
  - A.6.B
  - A.6.C
  - B.5.1
fpf.vocabulary_watch:
  - containment
  - truncation
  - redaction
  - hook timeout
acceptance:
  - Before each hook and worker launch, the harness rejects any workspace path that resolves outside `workspace.root`.
  - Automated tests prove that configured secret env values do not appear verbatim in operator-visible logs, run records, request files, or default transcripts.
  - Hook and external-process timeout, truncation/redaction, and non-zero-exit behavior are documented and covered by automated tests.
validation:
  - dotnet build src/DeliveryHarness.Cli/DeliveryHarness.Cli.fsproj
  - dotnet run --project tests/DeliveryHarness.Tests/DeliveryHarness.Tests.fsproj
constraints:
  - Keep the trusted-local model explicit.
  - Do not claim container or VM isolation that does not exist.
---
# Problem

The safety baseline described here has already landed. The starter now revalidates workspace containment before hook and worker launch, redacts workflow-configured secret-like env values from operator-visible summaries and default transcripts, and documents the current no-truncation-by-default output policy.

# Scope

Keep this issue as the historical reference for the current trusted-local safety baseline so later work extends the documented controls instead of re-describing them as missing.

# Notes

Execution plan:
- `docs/exec-plans/completed/DEMO-0010.md`

FPF validation:
- lifecycle state: `Shaping`
- admissibility gate: requires `DEMO-0002`
- contract bundle routing: acceptance = promise content, validation = evidence, constraints = admissibility/deontic limits
- vocabulary watch: `containment`, `truncation`, `redaction`, `hook timeout`

Use the existing workspace-root invariant as the baseline and extend it where runtime behavior still trusts too much implicit state.

Current baseline:
- hooks and workers revalidate workspace-root containment immediately before execution
- workflow-configured secret-like env values are deterministically redacted from operator-visible summaries and default transcripts
- deterministic tests cover containment, failure preservation, non-active gating, and hook/worker redaction behavior

---
id: DEMO-0010
title: Strengthen workspace, process, and secret-handling safety guards
state: Todo
priority: 2
depends_on:
  - DEMO-0002
fpf.lifecycle_state: Shaping
fpf.semantic_risk: high
fpf.primary_patterns:
  - A.15.2
  - A.6.B
  - A.6.C
  - B.5.1
fpf.vocabulary_watch:
  - containment
  - truncation
  - redaction
  - hook timeout
acceptance:
  - Before each hook and worker launch, the harness rejects any workspace path that resolves outside `workspace.root`.
  - Automated tests prove that configured secret env values do not appear verbatim in operator-visible logs, run records, request files, or default transcripts.
  - Hook and external-process timeout, truncation/redaction, and non-zero-exit behavior are documented and covered by automated tests.
validation:
  - dotnet build src/DeliveryHarness.Cli/DeliveryHarness.Cli.fsproj
  - dotnet test
constraints:
  - Keep the trusted-local model explicit.
  - Do not claim container or VM isolation that does not exist.
---
# Problem

The starter has basic path checks, but a real harness needs clearer safety guarantees around shell hooks, external processes, and secrets.

# Scope

Tighten the current safety invariants and make their failure modes visible to operators.

# Notes

Execution plan:
- `docs/exec-plans/active/DEMO-0010.md`

FPF validation:
- lifecycle state: `Shaping`
- admissibility gate: requires `DEMO-0002`
- contract bundle routing: acceptance = promise content, validation = evidence, constraints = admissibility/deontic limits
- vocabulary watch: `containment`, `truncation`, `redaction`, `hook timeout`

Use the existing workspace-root invariant as the baseline and extend it where runtime behavior still trusts too much implicit state.

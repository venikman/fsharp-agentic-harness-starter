---
id: DEMO-0002
title: Add deterministic automated validation and fixtures for the harness
state: Done
priority: 1
depends_on:
fpf.lifecycle_state: Evidence
fpf.semantic_risk: low
fpf.primary_patterns:
  - A.15.2
  - A.6.C
  - B.5.1
fpf.vocabulary_watch:
acceptance:
  - `dotnet build src/DeliveryHarness.Cli/DeliveryHarness.Cli.fsproj` succeeds on the documented .NET SDK before deeper changes begin.
  - A harness test project exists in the repo and `dotnet run --project tests/DeliveryHarness.Tests/DeliveryHarness.Tests.fsproj` runs without network access or external credentials.
  - Deterministic tests cover at least workflow loading, issue parsing/sorting, workspace path safety, and run-record or dry-run artifact behavior.
  - Repo docs and issue validations reference `dotnet run --project tests/DeliveryHarness.Tests/DeliveryHarness.Tests.fsproj` as the harness test entry point once the suite exists.
validation:
  - dotnet build src/DeliveryHarness.Cli/DeliveryHarness.Cli.fsproj
  - dotnet run --project tests/DeliveryHarness.Tests/DeliveryHarness.Tests.fsproj
constraints:
  - Keep the file-backed tracker and dry-run agent path usable in tests.
  - Avoid real tracker credentials and network dependencies in the default test suite.
---
# Problem

This Wave 0 baseline work has already landed. The starter now builds cleanly and the repo contains deterministic automated validation for workflow loading, issue parsing, workspace safety, run records, and worker-path artifacts.

# Scope

Keep this issue as the historical baseline reference so later backlog items extend the existing proving path instead of re-describing it as missing.

# Notes

Execution plan:
- `docs/exec-plans/active/DEMO-0002.md`

FPF validation:
- lifecycle state: `Evidence`
- admissibility gate: none; this is the first backlog gate
- contract bundle routing: acceptance = promise content, validation = evidence, constraints = admissibility/deontic limits
- vocabulary watch: none; low semantic risk at the issue-file level

This is the completed preparation step before deeper orchestration work. Later changes should preserve and extend the deterministic validation matrix for workflow/config, workspace safety, and orchestration basics.

Current baseline:
- `tests/DeliveryHarness.Tests/` exists and is exercised via `dotnet run --project tests/DeliveryHarness.Tests/DeliveryHarness.Tests.fsproj`
- repo docs and issue validations already reference `dotnet run --project tests/DeliveryHarness.Tests/DeliveryHarness.Tests.fsproj`
- later issues should extend the deterministic suite when they change runtime behavior

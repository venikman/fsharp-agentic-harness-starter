---
id: DEMO-0002
title: Add deterministic automated validation and fixtures for the harness
state: Todo
priority: 1
depends_on:
fpf.lifecycle_state: Evidence
fpf.semantic_risk: low
fpf.primary_patterns:
  - A.15.2
  - A.6.C
  - B.5.1
fpf.vocabulary_watch:
acceptance:
  - `dotnet build src/DeliveryHarness.Cli/DeliveryHarness.Cli.fsproj` succeeds on the documented .NET SDK before deeper changes begin.
  - A harness test project exists in the repo and `dotnet test` runs without network access or external credentials.
  - Deterministic tests cover at least workflow loading, issue parsing/sorting, workspace path safety, and run-record or dry-run artifact behavior.
  - Repo docs and issue validations reference `dotnet test` as the harness test entry point once the suite exists.
validation:
  - dotnet build src/DeliveryHarness.Cli/DeliveryHarness.Cli.fsproj
  - dotnet test
constraints:
  - Keep the file-backed tracker and dry-run agent path usable in tests.
  - Avoid real tracker credentials and network dependencies in the default test suite.
---
# Problem

The starter currently needs both a green build baseline and deterministic feedback before agents start changing orchestration behavior.

# Scope

Add automated tests and fixtures for the current local-first harness behavior.

# Notes

Execution plan:
- `docs/exec-plans/active/DEMO-0002.md`

FPF validation:
- lifecycle state: `Evidence`
- admissibility gate: none; this is the first backlog gate
- contract bundle routing: acceptance = promise content, validation = evidence, constraints = admissibility/deontic limits
- vocabulary watch: none; low semantic risk at the issue-file level

This is the first preparation step before deeper orchestration work. Align with the Symphony spec's validation matrix for workflow/config, workspace safety, and orchestration basics.

Current observed blocker:
- `dotnet build src/DeliveryHarness.Cli/DeliveryHarness.Cli.fsproj` fails on `src/DeliveryHarness.Core/Agent.fs` around `replaceTokens`, plus non-fatal F# warnings about the identifier `process` in `ProcessRunner.fs`.

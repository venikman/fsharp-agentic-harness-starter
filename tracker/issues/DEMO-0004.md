---
id: DEMO-0004
title: Introduce a tracker abstraction while preserving the file-backed adapter
state: Todo
priority: 2
depends_on:
  - DEMO-0002
fpf.lifecycle_state: Shaping
fpf.semantic_risk: high
fpf.primary_patterns:
  - A.15.2
  - A.6.C
  - B.5.1
  - F.9
fpf.vocabulary_watch:
  - tracker adapter
  - candidate issue
  - state refresh
  - terminal query
acceptance:
  - Orchestrator and CLI code call tracker operations through a tracker port or interface instead of referencing `FileTracker` directly.
  - The markdown file tracker still powers `list-issues` and `run-issue` when `tracker.kind: file` is configured.
  - The tracker boundary exposes explicit operations for candidate listing, issue lookup by id, state refresh, and terminal-issue enumeration.
validation:
  - dotnet build src/DeliveryHarness.Cli/DeliveryHarness.Cli.fsproj
  - dotnet test
  - dotnet run --project src/DeliveryHarness.Cli/DeliveryHarness.Cli.fsproj -- list-issues
constraints:
  - Keep the current issue markdown format working.
  - Do not require a networked tracker in this first pass.
---
# Problem

The starter hard-wires orchestration to the file-backed tracker, which makes later tracker integration larger and riskier than it needs to be.

# Scope

Create the tracker seam needed for future adapters while keeping the local markdown path intact.

# Notes

Execution plan:
- `docs/exec-plans/active/DEMO-0004.md`

FPF validation:
- lifecycle state: `Shaping`
- admissibility gate: requires `DEMO-0002`
- contract bundle routing: acceptance = promise content, validation = evidence, constraints = admissibility/deontic limits
- vocabulary watch: `tracker adapter`, `candidate issue`, `state refresh`, `terminal query`

Model the boundary after Symphony's candidate fetch, state refresh, and terminal cleanup responsibilities, but keep the first adapter file-backed.

---
id: DEMO-0001
title: Replace the starter dry-run agent with a real CLI
state: Done
priority: 1
depends_on:
  - DEMO-0002
fpf.lifecycle_state: Shaping
fpf.semantic_risk: medium
fpf.primary_patterns:
  - A.15.2
  - A.6.C
  - B.5.1
  - F.11
fpf.vocabulary_watch:
  - worker
  - dry-run
  - request file
  - transcript
acceptance:
  - When `agent.command` is not `dry-run`, `run-issue` invokes the configured external command with `cwd` set to the issue workspace.
  - The invoked command receives the expanded `{request_path}` argument and the referenced request file exists under `<workspace>/.harness/agent-request.md`.
  - The harness writes the command stdout/stderr transcript to a file under `<workspace>/.harness/` on both success and non-zero exit.
validation:
  - dotnet build src/DeliveryHarness.Cli/DeliveryHarness.Cli.fsproj
  - dotnet run --project src/DeliveryHarness.Cli/DeliveryHarness.Cli.fsproj -- run-issue ACTIVE_ISSUE_ID
constraints:
  - Keep the default `dry-run` path working.
  - Do not add external packages for this first pass.
---
# Problem

The generic external-worker path described here has already landed. The repo now supports non-`dry-run` commands with workspace-local request files, token-expanded arguments, and persisted transcripts.

# Scope

Keep this issue as the historical reference for the worker contract so later backlog items extend the existing path instead of re-describing it as missing.

# Notes

Execution plan:
- `docs/exec-plans/completed/DEMO-0001.md`

FPF validation:
- lifecycle state: `Shaping`
- admissibility gate: requires `DEMO-0002`
- contract bundle routing: acceptance = promise content, validation = evidence, constraints = admissibility/deontic limits
- vocabulary watch: `worker`, `dry-run`, `request file`, `transcript`

Current baseline:
- supported worker tokens: `{workspace}`, `{issue_id}`, `{issue_title}`, `{request_path}`, `{project_root}`

- non-`dry-run` commands run with `cwd = workspace`
- request files are written under `<workspace>/.harness/agent-request.md`
- stdout/stderr transcripts are persisted under `<workspace>/.harness/agent-output.txt`
- the default `dry-run` path remains the proving baseline

Boundary rule:
- If the configured external agent is `pi`, all runtime configuration must still remain harness-owned rather than living in checked-in `.pi/` files or pi package settings.

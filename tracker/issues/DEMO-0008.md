---
id: DEMO-0008
title: Reload `WORKFLOW.md` without restart and keep the last known good config
state: Done
priority: 3
depends_on:
  - DEMO-0005
fpf.lifecycle_state: Shaping
fpf.semantic_risk: high
fpf.primary_patterns:
  - A.15.2
  - A.6.C
  - B.5.1
  - F.11
fpf.vocabulary_watch:
  - hot-reload
  - last known good
  - active config
  - restart-required
acceptance:
  - In long-running mode, a valid `WORKFLOW.md` change is detected and applied without process restart.
  - An invalid `WORKFLOW.md` change leaves the previous effective workflow active and emits an operator-visible reload error.
  - Repo docs list which workflow settings are hot-reloaded and which require restart.
validation:
  - dotnet build src/DeliveryHarness.Cli/DeliveryHarness.Cli.fsproj
  - dotnet run --project tests/DeliveryHarness.Tests/DeliveryHarness.Tests.fsproj
constraints:
  - Do not lose the current prompt or config on a bad reload.
  - Keep explicit workflow-path selection behavior intact.
---
# Problem

The hot-reload behavior described here has already landed. Host mode now checks `WORKFLOW.md` for changes on each tick, applies valid hot-reloadable changes to future ticks/runs, and keeps the last known good workflow active when reloads are invalid or restart-required.

# Scope

Keep this issue as the historical reference for workflow reload semantics so later work extends the documented reload contract instead of re-describing it as missing.

# Notes

Execution plan:
- `docs/exec-plans/completed/DEMO-0008.md`

FPF validation:
- lifecycle state: `Shaping`
- admissibility gate: requires `DEMO-0005`
- contract bundle routing: acceptance = promise content, validation = evidence, constraints = admissibility/deontic limits
- vocabulary watch: `hot-reload`, `last known good`, `active config`, `restart-required`

Current baseline:
- prompt body, active/terminal states, cleanup policy, and orchestrator cadence/concurrency/retry settings are hot-reloaded for future ticks/runs
- tracker location, workspace root, worker command/args/timeout, `agent.max_turns`, and hooks remain restart-required
- invalid or restart-required edits leave the prior workflow active and surface `LastReloadError` through status/logs

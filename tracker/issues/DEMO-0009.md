---
id: DEMO-0009
title: Implement a Linear-compatible tracker adapter
state: Done
priority: 3
depends_on:
  - DEMO-0004
  - DEMO-0010
fpf.lifecycle_state: Shaping
fpf.semantic_risk: high
fpf.primary_patterns:
  - A.15.2
  - A.6.C
  - B.5.1
  - F.9
fpf.vocabulary_watch:
  - normalized issue model
  - candidate fetch
  - state refresh
  - terminal issue
acceptance:
  - A new tracker adapter implements candidate fetch, issue-state refresh, and terminal-issue listing against a Linear-compatible API shape.
  - Required tracker credentials are read from environment-backed harness config and are not checked into the repo.
  - The markdown file tracker remains usable for local proving and for `tracker.kind: file`.
validation:
  - dotnet build src/DeliveryHarness.Cli/DeliveryHarness.Cli.fsproj
constraints:
  - Keep tracker writes out of the core orchestrator for this pass.
  - Document required environment variables and trust assumptions.
---
# Problem

The Linear-compatible read path described here has already landed. The starter now supports `tracker.kind: linear` with env-backed `tracker.api_key`, a default official GraphQL endpoint, paginated issue listing/candidate fetch/terminal listing, and per-issue refresh while preserving the file-backed proving path.

# Scope

Keep this issue as the historical reference for the current external-tracker read baseline so later work extends the tracker seam instead of re-describing this adapter as missing.

# Notes

Execution plan:
- `docs/exec-plans/completed/DEMO-0009.md`

FPF validation:
- lifecycle state: `Shaping`
- admissibility gate: requires `DEMO-0004` and `DEMO-0010`
- contract bundle routing: acceptance = promise content, validation = evidence, constraints = admissibility/deontic limits
- vocabulary watch: `normalized issue model`, `candidate fetch`, `state refresh`, `terminal issue`

Current baseline:
- `tracker.kind: linear` uses `tracker.project_key` as the Linear team key and requires `tracker.api_key` from an environment-variable reference
- the adapter normalizes identifier, title, description, state, priority, updated time, and source URL into the harness-owned issue model
- deterministic validation cover pagination, state-filter queries, other-team filtering, and GraphQL error propagation
- tracker writes remain out of scope and the file-backed adapter still works unchanged for the local proving path

---
id: DEMO-0006
title: Reconcile running issues against tracker state and clean up terminal workspaces
state: Done
priority: 1
depends_on:
  - DEMO-0005
  - DEMO-0004
fpf.lifecycle_state: Shaping
fpf.semantic_risk: high
fpf.primary_patterns:
  - A.15.2
  - A.6.C
  - B.5.1
  - F.9
fpf.vocabulary_watch:
  - active state
  - terminal state
  - non-active state
  - reconciliation
  - cleanup
acceptance:
  - On each host tick, running issues are refreshed through the tracker seam before reconciliation decisions are made.
  - If a running issue enters a terminal state, active work stops and workspace cleanup follows `workspace.cleanup_terminal`.
  - If a running issue leaves the active set without entering a terminal state, active work stops and the workspace is preserved.
validation:
  - dotnet build src/DeliveryHarness.Cli/DeliveryHarness.Cli.fsproj
constraints:
  - Preserve the existing terminal workspace cleanup policy switch.
  - Keep file-backed tracker behavior working while the runtime grows.
---
# Problem

The live-run reconciliation described here has already landed. Host mode now refreshes running issues through the tracker seam on each tick, cancels work that becomes terminal or otherwise non-active, and keeps workspace cleanup policy explicit.

# Scope

Keep this issue as the historical reference for reconciliation semantics so later work extends the existing tracker-refresh and cleanup contract instead of treating it as missing.

# Notes

Execution plan:
- `docs/exec-plans/completed/DEMO-0006.md`

FPF validation:
- lifecycle state: `Shaping`
- admissibility gate: requires `DEMO-0005` and `DEMO-0004`
- contract bundle routing: acceptance = promise content, validation = evidence, constraints = admissibility/deontic limits
- vocabulary watch: `active state`, `terminal state`, `non-active state`, `reconciliation`, `cleanup`

Current baseline:
- running issues are refreshed through `Tracker.tryRefreshById` before reconciliation decisions are made
- terminal transitions cancel work and respect `workspace.cleanup_terminal`
- non-active, non-terminal transitions cancel work without destructive cleanup
- startup and steady-state terminal cleanup both stay policy-driven rather than tracker-write-driven

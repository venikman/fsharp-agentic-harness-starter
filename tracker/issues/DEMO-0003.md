---
id: DEMO-0003
title: Replace repo-copy workspace bootstrap with git-based workspace provisioning
state: Done
priority: 2
depends_on:
  - DEMO-0002
fpf.lifecycle_state: Shaping
fpf.semantic_risk: medium
fpf.primary_patterns:
  - A.15.2
  - A.6.C
  - B.5.1
fpf.vocabulary_watch:
  - workspace bootstrap
  - workspace reuse
  - reset policy
acceptance:
  - `tools/AfterCreate.fsx` provisions a new workspace from git (`git worktree` or a documented clone-based fallback) instead of recursively copying the repo tree.
  - Repo docs name the reused-workspace policy and state whether reused workspaces are left unchanged, synchronized, or rejected.
  - On bootstrap failure, the workspace `.harness/after-create.txt` file or its documented replacement records the failure and the bootstrap step does not mutate the repo root.
validation:
  - dotnet build src/DeliveryHarness.Cli/DeliveryHarness.Cli.fsproj
  - dotnet run --project tests/DeliveryHarness.Tests/DeliveryHarness.Tests.fsproj
  - dotnet run --project src/DeliveryHarness.Cli/DeliveryHarness.Cli.fsproj -- run-issue ACTIVE_ISSUE_ID
constraints:
  - Work only inside the configured workspace root.
  - Do not require containers or remote hosts in this first pass.
---
# Problem

The git-backed workspace baseline described here has already landed. The starter now provisions workspaces through `git worktree`, keeps reuse policy explicit, and records bootstrap outcomes under workspace-local `.harness/` files.

# Scope

Keep this issue as the historical reference for workspace provisioning so later work preserves the hook boundary and explicit reuse/reset policy instead of re-describing repo-copy bootstrap as current behavior.

# Notes

Execution plan:
- `docs/exec-plans/active/DEMO-0003.md`

FPF validation:
- lifecycle state: `Shaping`
- admissibility gate: requires `DEMO-0002`
- contract bundle routing: acceptance = promise content, validation = evidence, constraints = admissibility/deontic limits
- vocabulary watch: `workspace bootstrap`, `workspace reuse`, `reset policy`

Current baseline:
- `tools/AfterCreate.fsx` provisions new workspaces with `git worktree`
- reused workspaces are left unchanged unless explicitly removed
- `tools/BeforeRemove.fsx` removes git-worktree-backed workspaces when cleanup is enabled
- bootstrap/cleanup behavior stays behind the hook-driven boundary rather than moving into `DeliveryHarness.Core`

---
id: MDP-0010
title: Publish reference-case evidence and package the library for initial release
state: Todo
priority: 3
depends_on:
  - MDP-0009
fpf.lifecycle_state: Evidence
fpf.semantic_risk: medium
fpf.primary_patterns:
  - A.6.C
  - A.15.2
  - B.5.1
fpf.vocabulary_watch:
  - reference case
  - release build
  - package
  - supported feature set
acceptance:
  - `docs/reference-cases/` records representative input/output cases for the supported dialect slice.
  - The repo documents the exact supported feature set, known exclusions, and release usage examples.
  - The library builds in release mode and the CLI smoke surface remains usable for evidence gathering.
validation:
  - dotnet build src/MarkdownParser/MarkdownParser.fsproj -c Release
  - dotnet run --project src/MarkdownParser.Cli/MarkdownParser.Cli.fsproj -- --format html samples/showcase.md
  - dotnet run --project src/MarkdownParser.Cli/MarkdownParser.Cli.fsproj -- --format ast samples/showcase.md
constraints:
  - Evidence should use real reference cases for the supported dialect, not placeholder claims of coverage.
  - Release docs must describe only implemented behavior.
---
# Problem

An initial release needs honest evidence, not just implemented code paths. Without reference cases and packaging-level proof, the repo cannot state what it really supports.

# Scope

Publish reference cases for the supported dialect slice, document the exact supported feature set and exclusions, and prove the release build/package surface.

# Notes

Execution plan:
- `docs/exec-plans/active/MDP-0010.md`

FPF validation:
- lifecycle state: `Evidence`
- admissibility gate: requires `MDP-0009`
- contract bundle routing: acceptance = promise content, validation = evidence, constraints = admissibility/deontic limits
- vocabulary watch: `reference case`, `release build`, `package`, `supported feature set`

Recommended starting paths:
- `docs/reference-cases/`
- `README.md`
- `src/MarkdownParser/`

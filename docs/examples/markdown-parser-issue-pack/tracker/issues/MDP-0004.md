---
id: MDP-0004
title: Parse paragraphs, headings, and thematic breaks
state: Todo
priority: 1
depends_on:
  - MDP-0003
fpf.lifecycle_state: Shaping
fpf.semantic_risk: medium
fpf.primary_patterns:
  - A.15.2
  - B.5.1
fpf.vocabulary_watch:
  - paragraph
  - ATX heading
  - setext heading
  - thematic break
acceptance:
  - Paragraphs parse into stable AST nodes.
  - ATX headings, setext headings, and thematic breaks are supported according to the chosen dialect boundary.
  - The CLI smoke output makes heading levels and block boundaries inspectable.
validation:
  - dotnet build src/MarkdownParser/MarkdownParser.fsproj
  - dotnet run --project src/MarkdownParser.Cli/MarkdownParser.Cli.fsproj -- samples/headings-and-breaks.md
constraints:
  - Stay inside the dialect slice declared in `docs/DIALECT.md`.
  - Prefer explicit unsupported-case notes over ambiguous parsing.
---
# Problem

The parser needs its first useful block forms so humans can inspect real output and assess whether the AST and block foundation are working. Paragraphs and headings are the smallest meaningful block set for early review.

# Scope

Implement paragraphs, ATX headings, setext headings, and thematic breaks within the chosen dialect boundary.

# Notes

Execution plan:
- `docs/exec-plans/active/MDP-0004.md`

FPF validation:
- lifecycle state: `Shaping`
- admissibility gate: requires `MDP-0003`
- contract bundle routing: acceptance = promise content, validation = evidence, constraints = admissibility/deontic limits
- vocabulary watch: `paragraph`, `ATX heading`, `setext heading`, `thematic break`

Recommended starting paths:
- `src/MarkdownParser/`
- `src/MarkdownParser.Cli/`
- `samples/headings-and-breaks.md`

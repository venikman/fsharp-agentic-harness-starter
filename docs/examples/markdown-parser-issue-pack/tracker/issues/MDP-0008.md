---
id: MDP-0008
title: Parse lists and block quotes with nested structure
state: Todo
priority: 2
depends_on:
  - MDP-0004
  - MDP-0005
  - MDP-0006
fpf.lifecycle_state: Shaping
fpf.semantic_risk: high
fpf.primary_patterns:
  - A.15.2
  - B.5.1
fpf.vocabulary_watch:
  - ordered list
  - unordered list
  - block quote
  - nesting
acceptance:
  - Ordered lists, unordered lists, and block quotes parse into nested AST structure.
  - Nesting behavior is deterministic and documented for the chosen dialect slice.
  - The CLI smoke output shows nested structure clearly enough for review.
validation:
  - dotnet build src/MarkdownParser/MarkdownParser.fsproj
  - dotnet run --project src/MarkdownParser.Cli/MarkdownParser.Cli.fsproj -- samples/lists-and-quotes.md
constraints:
  - Keep nesting rules explicit.
  - Avoid mixing renderer heuristics into parser structure decisions.
---
# Problem

Nested block structure is where indentation and continuation rules start to matter. Lists and block quotes need their own bounded step so the parser's structural semantics stay reviewable.

# Scope

Implement ordered lists, unordered lists, and block quotes with deterministic nesting for the chosen dialect slice.

# Notes

Execution plan:
- `docs/exec-plans/active/MDP-0008.md`

FPF validation:
- lifecycle state: `Shaping`
- admissibility gate: requires `MDP-0004`, `MDP-0005`, and `MDP-0006`
- contract bundle routing: acceptance = promise content, validation = evidence, constraints = admissibility/deontic limits
- vocabulary watch: `ordered list`, `unordered list`, `block quote`, `nesting`

Recommended starting paths:
- `docs/DIALECT.md`
- `src/MarkdownParser/`
- `samples/lists-and-quotes.md`

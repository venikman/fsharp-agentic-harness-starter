# Execution plan

## Issue

`DEMO-0001`

## Status

Completed baseline reference retained until plan archival exists.

## Outcome

`src/DeliveryHarness.Core/Agent.fs` already provides a harness-controlled external worker path alongside `dry-run`. The harness writes `agent-request.md`, expands the supported tokens, runs the worker with `cwd = workspace`, and persists stdout/stderr transcripts under workspace `.harness/` on success, failure, and timeout paths.

## What later work should assume

- the generic external worker path is baseline behavior, not future work
- `dry-run` remains the default proving path
- worker configuration stays harness-owned in `WORKFLOW.md` and harness-managed environment variables

## Follow-on scope

- `DEMO-0010` for tighter failure, redaction, and safety behavior
- `DEMO-0011` for richer turn semantics
- `DEMO-0012` for stricter prompt templating

## Validation reference

- `dotnet build src/DeliveryHarness.Cli/DeliveryHarness.Cli.fsproj`
- `dotnet run --project tests/DeliveryHarness.Tests/DeliveryHarness.Tests.fsproj`

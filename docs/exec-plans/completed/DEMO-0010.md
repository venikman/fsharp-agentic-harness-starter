# Execution plan

## Issue

`DEMO-0010`

## Status

Completed baseline reference retained until plan archival exists.

## Outcome

The current starter already implements the safety baseline this plan targeted: hooks and worker launches revalidate workspace containment, operator-visible output is deterministically redacted for workflow-configured secret-like env values, and the repo now documents the no-truncation-by-default local-first output policy.

## What later work should assume

- workspace containment before hook and worker launch is baseline behavior
- operator-visible redaction for workflow-configured secret-like env values is baseline behavior
- later work should extend this trusted-local safety contract rather than re-describing it as missing

## Expected deliverables

- enforced pre-execution workspace containment checks
- documented and tested hook/process timeout/failure behavior
- reduced accidental secret exposure in operator-visible artifacts
- docs reflecting the real, not imagined, safety posture

## Follow-on scope

- `DEMO-0007` should preserve the same redaction rules when structured logging arrives
- `DEMO-0009` should reuse the same env-backed secret-handling posture for external tracker credentials
- later hardening may add truncation or stronger isolation only when the runtime actually implements it

## Validation

- `dotnet build src/DeliveryHarness.Cli/DeliveryHarness.Cli.fsproj`

## Handoff expectations

A review-ready result should include:
- the specific invariants that are now enforced before execution
- validation demonstrating containment, timeout, failure, and redaction behavior
- notes on the current no-truncation-by-default output policy
- an explicit statement that container/VM isolation is still out of scope

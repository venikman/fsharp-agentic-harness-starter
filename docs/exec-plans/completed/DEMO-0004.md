# Execution plan

## Issue

`DEMO-0004`

## Status

Completed baseline reference retained until plan archival exists.

## Outcome

`src/DeliveryHarness.Core/Tracker.fs` now provides the harness-owned tracker seam this plan was meant to introduce. CLI and orchestration call through that boundary, and the default adapter still serves markdown issues from `tracker/issues/*.md`.

## What later work should assume

- the tracker seam is baseline behavior, not future work
- the markdown tracker remains the default local proving adapter
- later adapters should plug into the existing boundary instead of bypassing it

## Expected deliverables

- a stable tracker seam in core code
- file-backed behavior preserved behind the seam
- tests that describe tracker behavior expected by the harness
- docs clarifying that later adapters plug into this boundary

## Follow-on scope

- `DEMO-0005` and `DEMO-0006` may now depend on the tracker seam for host-mode orchestration and reconciliation
- `DEMO-0009` may add a non-file adapter without changing the orchestrator contract

## Validation

- `dotnet build src/DeliveryHarness.Cli/DeliveryHarness.Cli.fsproj`
- `dotnet run --project tests/DeliveryHarness.Tests/DeliveryHarness.Tests.fsproj`
- `dotnet run --project src/DeliveryHarness.Cli/DeliveryHarness.Cli.fsproj -- list-issues`

## Handoff expectations

A review-ready result should include:
- the tracker seam and its intended responsibilities
- confirmation that the markdown tracker remains the default adapter
- a short list of behaviors protected by tests
- any intentionally deferred tracker features reserved for later issues such as `DEMO-0009`

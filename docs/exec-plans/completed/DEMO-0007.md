# Execution plan

## Issue

`DEMO-0007`

## Status

Completed baseline reference retained until plan archival exists.

## Outcome

Host mode now appends structured runtime events to `.harness/runtime/host-events.jsonl`, writes `.harness/runtime/status.json`, and exposes `status` as a read-only CLI projection of the latest snapshot.

## What later work should assume

- structured host events are baseline behavior
- running and retrying state can be inspected through the JSON snapshot and `status`
- issue-related log entries carry stable issue/attempt/turn context when applicable
- observability remains optional for correctness and preserves the existing redaction posture

## Expected deliverables

- structured host event log
- JSON status snapshot plus CLI reader
- deterministic validation for representative log/status behavior
- docs describing where operators inspect runtime state

## Follow-on scope

- future dashboards or APIs should treat the snapshot/log as read-only projections, not a control plane
- later tracker or continuation work should preserve the same redaction and context-field rules

## Validation

- `dotnet build src/DeliveryHarness.Cli/DeliveryHarness.Cli.fsproj`

## Handoff expectations

A review-ready result should include:
- the log and snapshot artifact paths
- the context fields available for issue/run correlation
- notes on the preserved redaction posture
- confirmation that the host still functions without a UI or dashboard

# Execution plan

## Issue

`DEMO-0002`

## Status

Completed baseline reference retained until plan archival exists.

## Outcome

The repository already has the Wave 0 proving path this plan was meant to create: the build baseline is green, `tests/DeliveryHarness.Tests/` provides deterministic harness validation, and the repo docs already use `dotnet run --project tests/DeliveryHarness.Tests/DeliveryHarness.Tests.fsproj` as the standard behavioral validation entry point.

## What later work should assume

- deterministic tests are baseline behavior, not future work
- new runtime behavior should land with focused test coverage in the existing suite
- repo-owned docs and `WORKFLOW.md` remain the source of truth for the proving path

## Follow-on scope

- preserve and extend the deterministic suite when worker, workspace, safety, or orchestration behavior changes
- keep the local file-backed and `dry-run` proving paths usable until later issues explicitly replace them

## Validation reference

- `dotnet restore src/DeliveryHarness.Cli/DeliveryHarness.Cli.fsproj`
- `dotnet build src/DeliveryHarness.Cli/DeliveryHarness.Cli.fsproj`
- `dotnet run --project tests/DeliveryHarness.Tests/DeliveryHarness.Tests.fsproj`
- `dotnet run --project src/DeliveryHarness.Cli/DeliveryHarness.Cli.fsproj -- validate-workflow`
